# 搜索功能优化日志

## 背景

首页搜索（`index.html` 内嵌逻辑）原本是**纯精确子串匹配**：整个查询作为一个整体在 `text`/标题里 `indexOf`。局限：

- **多词 / 词序**：查询当作一个整体，"极限 连续""线性 代数"（多词、乱序）都搜不到；
- **英文术语 typo**：拼错一个字母就找不到（"banah" → "Banach"）；
- 无拼音、无输入防抖。

## 调研结论（联网学习）

- **CJK 搜索共识**（Pagefind issue #987）：对中文,**子串匹配**（像 `"关键词" in 文本`）比分词更符合母语者预期——分词反而会漏掉跨词的子串。所以原项目的子串方向是对的。
- **Fuse.js 等库**：字符级 fuzzy 对中文噪声大、在 3000+ 块上偏慢、且要引入一个库,不符合本站零依赖、单文件 `index.html` 的极简风格。
- **`Intl.Segmenter`**：浏览器原生中文分词,但按上面的共识,分词对匹配不一定更好。
- **结论**：不引库,自研轻量逻辑内联 —— 中文继续用子串（+ 多词 AND）,英文叠加编辑距离容错,中文标题叠加拼音。

## 新增能力

| 能力 | 例子 | 说明 |
| --- | --- | --- |
| 多词任意顺序（AND） | `极限 连续` / `连续 极限` | 按空格拆词,每个词都命中即可,顺序无关 |
| 拼音首字母 | `tjgl` → 条件概率 | 中文标题/章节的首字母,支持子串（`xxhg` → 简单线性回归模型） |
| 拼音全拼 | `jixian` → 中心极限定理 | 标题/章节的全拼 |
| 英文术语 typo 容忍 | `banah` → Banach,`eigenvalu` → eigenvalue | 编辑距离 ≤ 1,仅对英文（中文字符增删无意义） |
| 前缀 / 子串 | `特征` → 特征值 | 子串匹配天然含前缀 |
| 精确短语加权 | `条件概率` | 整句连续命中额外加分,排在分散命中之前 |
| 输入防抖 + 多词高亮 | — | 120ms 防抖,结果里每个命中词分别高亮 |
| 全角转半角归一化 | `ＳＶＤ` → `svd` | 统一全/半角、大小写 |

## 实现

**前端（`index.html`,自研零依赖）**：
- `normalizeText`：小写 + 全角转半角（含全角空格）；
- `withinEditDistance` / `latinTypoHit`：英文词编辑距离 ≤ 1 容错（带逐行剪枝）；
- `tokenFieldScore`：单个词在一个块上的最佳命中分（子串 → 拼音 → 英文容错的优先级）；
- `buildSearchFields`：把正文、显式块标题、子章节、章节、书名拆成不同字段；如果 `blockTitle` 只是从当前章节/子章节继承而来，不按“块标题”给高分；
- `scoreItem`：多词 **AND**（任一词不命中即淘汰）+ 字段权重 + kind 加权 + 精确短语 bonus + 长度偏好；
- `makeSnippet`：围绕首个命中词截取；先在原文用占位符标记命中词、再转义 HTML、最后替换为 `<mark>`,既避免误伤转义产生的 HTML 实体（如 `&amp;`）、也不产生嵌套高亮；
- `searchNotes`：分词 → 打分 → 排序 → 每 repo 最多 3 条、共 12 条（沿用原限制）；
- 搜索框输入加 120ms 防抖。

**拼音数据（`scripts/add_pinyin.py`）**：
- 作为 `public/search-index.previews.json` 的后处理,用 `pypinyin` 为每个块的**标题类字段**（`previewTitle`/`blockTitle`/`section`/`subsection`,且含中文）生成 `pinyinInitials` 和 `pinyinFull`；
- 正文不做拼音（全文拼音过大、首字母噪声高,几乎任意字母串都能命中）；
- 独立于 enrich/SyncTeX 步骤,本地可直接跑。

**CI（`.github/workflows/build.yml`）**：在 “Render search previews” 之后新增 “Add pinyin search index” 步骤；`requirements.txt` 增加 `pypinyin`。

## 字段命中权重

| 命中位置 | 分值 |
| --- | --- |
| 正文子串 | 100 |
| 显式块标题子串 | 80 |
| 拼音首字母 | 55 |
| 子章节子串 | 45 |
| 拼音全拼 | 42 |
| 章节子串 | 35 |
| 英文标题/章节 typo | 36 |
| 书名/仓库名子串 | 15 |
| 正文英文 typo | 22 |
| 精确短语 bonus（正文/显式标题/子章节/章节/书名） | +50 / +35 / +15 / +15 / +8 |

`kind` 额外加分：`dxtips` +40，`definition` / `theorem` / `lemma` / `proposition` +20，`corollary` / `example` / `remark` / `note` +12。这个加分只负责同等相关性下的偏好，不替代字段相关性；例如正文真正出现“格林公式”的条目会排在只继承“格林公式”章节名的块前面。

## 验证

用 node 对真实 `previews.json` 跑查询,核心能力全部通过：

- `极限 连续` / `连续 极限` → 同结果（多词 AND、乱序无关）；
- `tjgl` → 条件概率,`xxhg` → 简单线性回归模型,`jixian` → 中心极限定理（拼音）；
- `banah` → Hahn-Banach 定理,`eigenvalu` → eigenvalue（英文 typo）；
- `特征值` / `条件概率` → 精确命中。
- `格林公式` → 真正正文命中的格林公式定义优先，章节下但正文不相关的“调和函数平均值性质”不再误排第一；
- `紧` → 正式定义/定理优先，避免单字正文命中被 `dxtips` 类型分过度放大。

JS 语法经 `node --check` 通过；无对已删除函数的残留引用。

## 设计取舍

- **拼音只索引标题/章节**：搜不到“没有中文标题、关键词只在正文”的块（如某些 `线性代数` 内容标题是英文 kind 名）。这是为控制体积/噪声的有意取舍。
- **typo 仅英文、距离 ≤ 1**：覆盖最常见的单字符拼错（漏/多/错一个字母）；字母乱序（编辑距离 2,如 `thereom`）不在容错范围。
- **多词用 AND 而非 OR**：更精准、噪声更少；代价是错字的那个词会让整条查询落空（由拼音/typo 兜底部分情况）。
